// Intentionally left empty to avoid DOM mutations that interfere with the Controls addon.
// This file previously injected a custom "Search props" input and filtered rows in the
// controls panel. That behavior caused issues after Storybook upgrades, so it is disabled.


